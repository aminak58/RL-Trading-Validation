# Vast AI RL Trading Framework Launcher

این اسکریپت **فقط** مدیریت زیرساخت Vast AI را برای اجرای فریمورک RL trading انجام می‌دهد.

## 📋 وظایف اسکریپت

✅ **انجام می‌دهد:**
- ایجاد خودکار اینستنس Vast AI مناسب با IP غیر آمریکایی
- نصب کامل ابستری‌های مورد نیاز (Python, PyTorch, Stable-Baselines3, FreqTrade)
- کپی پروژه روی اینستنس
- اجرای فریمورک با تنظیمات موجود
- کپی کامل نتایج شامل مدل‌های آموزش داده شده به سیستم محلی
- خاموش کردن خودکار اینستنس با تایید کاربر

❌ **انجام نمی‌دهد:**
- تغییر پارامترهای ترید (timeframe, timerange, strategy)
- تغییر تنظیمات مدل RL
- دستکاری در فایل‌های کانفیگ فریمورک

## 📋 پیش‌نیازها

### 1. نصب Vast AI CLI
```bash
pip install vastai
vastai set api-key YOUR_API_KEY
```

### 2. تنظیم SSH
اسکریپت از SSH key موجود در `C:/Users/Kiyan-System/.ssh/vast_fix` استفاده می‌کند.

### 3. فایل‌های مورد نیاز
- `vast_ai_launcher.py` - اسکریپت اصلی
- `scripts/run_backtest_with_analysis.py` - اسکریپت تحلیل فریمورک
- `user_data/` - پوشه استراتژی‌ها و مدل‌ها

## 🚀 نحوه استفاده

### روش 1: استفاده از فایل کانفیگ
```bash
# کپی فایل کانفیگ نمونه
cp vast_ai_config_example.json vast_ai_config.json

# اجرای فریمورک
python vast_ai_launcher.py --config vast_ai_config.json
```

### روش 2: استفاده از خط فرمان
```bash
# اجرا با تنظیمات پیش‌فرض
python vast_ai_launcher.py

# اجرا با مشخصات سخت‌افزاری دلخواه
python vast_ai_launcher.py --gpu-count 1 --min-gpu-ram 24 --max-price 0.4
```

### روش 3: گزینه‌های پیشرفته
```bash
# اجرا بدون درخواست خاموش کردن (برای دیباگ طولانی)
python vast_ai_launcher.py --no-terminate

# اجرا بدون کپی نتایج
python vast_ai_launcher.py --skip-copy

# محدودیت زمانی اجرا
python vast_ai_launcher.py --runtime-hours 12

# ترکیب گزینه‌ها
python vast_ai_launcher.py \
    --gpu-count 2 \
    --min-gpu-ram 24 \
    --max-price 0.6 \
    --runtime-hours 6
```

### 🔴 مدیریت اینستنس در انتهای اجرا

اسکریپت در انتهای اجرا به صورت خودکار از شما تایید می‌گیرد:

```
============================================================
🎯 FRAMEWORK EXECUTION COMPLETED
============================================================
Instance ID: 27742453
Instance IP: 123.45.67.89
💻 GPU: RTX 3090
💰 Current cost: $0.134/hour

============================================================
🔴 INSTANCE TERMINATION CONFIRMATION
============================================================
Instance ID: 27742453
Instance IP: 123.45.67.89

⚠️  This will permanently terminate the instance.
⚠️  All data on the instance will be lost.
⚠️  This action cannot be undone.

💰 Current cost: $0.134/hour
💻 GPU: RTX 3090

============================================================
Do you want to terminate this instance? (yes/no):
```

**گزینه‌ها:**
- `yes` یا `y`: خاموش کردن اینستنس
- `no` یا `n`: اینستنس فعال باقی می‌ماند

## ⚙️ تنظیمات کانفیگ

### بخش vastai (سخت‌افزار)
```json
{
  "vastai": {
    "min_gpu_count": 1,           // حداقل تعداد GPU
    "min_gpu_ram_gb": 16,         // حداقل VRAM هر GPU به گیگابایت **مهم**
    "min_disk_gb": 50,            // حداقل فضای دیسک به گیگابایت
    "max_price_per_hour": 0.5,    // حداکثر قیمت در ساعت
    "preferred_gpu_types": [      // انواع GPU ترجیحی
      "RTX 3090", "RTX 4090", "A4000", "A5000"
    ],
    "preferred_regions": [        // مناطق ترجیحی **برای Binance**
      "EU", "Asia", "Canada", "UK"
    ],
    "excluded_regions": [         // مناطق مسدود شده
      "US", "PR"
    ],
    "image": "nvidia/cuda:12.1.1-devel-ubuntu22.04"  // Docker image
  }
}
```

### بخش execution (اجرا)
```json
{
  "execution": {
    "max_runtime_hours": 24,    // حداکثر زمان اجرا
    "auto_terminate": true,     // خاموش کردن خودکار اینستنس
    "copy_results": true,       // کپی نتایج به محلی
    "cleanup_temp": true,       // پاکسازی فایل‌های موقت
    "check_binance_access": true // بررسی دسترسی به Binance
  }
}
```

## 📊 خروجی‌ها

### نتایج محلی
پس از اتمام اجرا، نتایج در پوشه `results/` با نام `run_YYYYMMDD_HHMMSS/` ذخیره می‌شوند:

```
results/
└── run_20241110_143022/
    ├── backtest_results.json
    ├── analysis_report.html
    ├── trained_models/           # مدل‌های آموزش داده شده RL
    │   ├── rl_model.zip
    │   └── model_metadata.json
    ├── collected_data/
    │   ├── trades.csv
    │   ├── rl_episodes.json
    │   └── features.csv
    └── logs/
        ├── vast_ai_launcher.log
        └── backtest.log
```

### لاگ‌ها
- `vast_ai_launcher.log` - لاگ اصلی اسکریپت زیرساخت
- `results/run_*/logs/` - لاگ‌های اجرای فریمورک

## 🌍 فیلتر کردن جغرافیایی برای Binance

### 🔴 مهم: دسترسی به Binance
بایننس ترافیک آمریکا را مسدود کرده است. اسکریپت به صورت خودکار اینستنس‌ها را فیلتر می‌کند:

- **مناطق ترجیحی**: EU, Asia, Canada, UK
- **مناطق مسدود شده**: US, PR
- **بررسی خودکار IP**: پس از ساخت اینستنس، موقعیت جغرافیایی IP بررسی می‌شود

### تنظیمات جغرافیایی در کانفیگ:
```json
{
  "vastai": {
    "preferred_regions": ["EU", "Asia", "Canada", "UK"],
    "excluded_regions": ["US", "PR"]
  }
}
```

## 🎯 مشخصات سخت‌افزاری پیشنهادی

### برای تست سریع:
```bash
python vast_ai_launcher.py --gpu-count 1 --min-gpu-ram 16 --max-price 0.3
```
- RTX 3090 (24GB VRAM) یا A4000 (16GB VRAM) در مناطق غیر آمریکایی
- مناسب برای تست‌های سریع (۱-۲ ساعت)

### برای اجرای کامل:
```bash
python vast_ai_launcher.py --gpu-count 1 --min-gpu-ram 24 --max-price 0.5
```
- RTX 3090 (24GB VRAM) یا RTX 4090 (24GB VRAM) در مناطق غیر آمریکایی
- مناسب برای اجراهای کامل (۳-۶ ساعت)

### برای مدل‌های بزرگ:
```bash
python vast_ai_launcher.py --gpu-count 2 --min-gpu-ram 24 --max-price 0.8
```
- 2x RTX 3090 یا 2x RTX 4090 در مناطق غیر آمریکایی
- مناسب برای مدل‌های بزرگ و آموزش طولانی

## 🔧 عیب‌یابی

### مشکلات رایج

#### 1. خطای SSH Key
```
Error: Permission denied (publickey)
```
**حل:** بررسی کنید SSH key در مسیر صحیح و با دسترسی مناسب باشد.

#### 2. خطای Vast AI CLI
```
Error: Vast AI CLI not found
```
**حل:**
```bash
pip install vastai
vastai set api-key YOUR_API_KEY
```

#### 3. پیدا نشدن اینستنس مناسب
```
Error: No suitable instances found
```
**حل:** تنظیمات را بازبینی کنید:
- افزایش `max_price_per_hour`
- کاهش `min_gpu_ram_gb` (حداقل 16)
- تغییر `preferred_gpu_types`

#### 4. خطای نصب پکیج‌ها
```
Error: Setup command failed
```
**حل:**
- اتصال اینترنت اینستنس را بررسی کنید
- Docker image را به نسخه جدیدتر تغییر دهید

#### 5. timeout در اجرا
```
Error: Framework execution timed out
```
**حل:**
- افزایش `max_runtime_hours`
- کاهش داده‌های ورودی در کانفیگ فریمورک

#### 6. مشکل دسترسی به Binance
```
Error: Binance access blocked - US IP detected
```
**حل:**
- بررسی کنید که `excluded_regions` شامل "US" و "PR" باشد
- از `preferred_regions` با مناطق غیر آمریکایی استفاده کنید
- لاگ جغرافیایی IP را در `vast_ai_launcher.log` بررسی کنید

#### 7. خطای نصب پیشرفته‌ها
```
Error: Advanced dependencies installation failed
```
**حل:**
- اتصال اینترنت اینستنس را بررسی کنید
- Docker image را به نسخه جدیدتر تغییر دهید
- لاگ نصب در `vast_ai_launcher.log` را چک کنید

## 📋 لیست دستورات مفید

### مدیریت اینستنس‌ها
```bash
# نمایش اینستنس‌های فعال
vastai show instances

# جستجوی اینستنس‌های مناسب (با حداقل 16GB VRAM)
vastai search offers 'num_gpus=1 gpu_ram>=16 dph<=0.5'

# حذف اینستنس
vastai destroy instance INSTANCE_ID

# نمایش اطلاعات حساب
vastai show user
```

### بررسی نتایج
```bash
# نمایش خلاصه نتایج
python -c "
import json
try:
    with open('results/run_*/analysis_report.json') as f:
        data = json.load(f)
        print(f'Total Profit: ${data[\"total_profit\"]:.2f}')
        print(f'Win Rate: {data[\"win_rate\"]:.1%}')
        print(f'Total Trades: {data[\"total_trades\"]}')
except:
    print('No results found yet')
"

# تحلیل داده‌های جمع‌آوری شده
python -c "
from user_data.data_collector import analyze_session
try:
    analyze_session('results/run_*/collected_data/')
except:
    print('No collected data found yet')
"
```

## 💡 نکات مهم

### VRAM (حافظه ویدیویی)
- **حداقل مورد نیاز**: 16GB VRAM
- **پیشنهادی**: 24GB VRAM (RTX 3090/4090)
- مدل‌های RL نیاز به VRAM زیادی دارند

### مدیریت هزینه و اینستنس
- از `max_price_per_hour` پایین‌تر استفاده کنید
- زمان اجرا را با `max_runtime_hours` محدود کنید
- **🔴 مهم**: در انتهای اجرا از شما تایید می‌خواهد که آیا اینستنس خاموش شود یا نه
- با `--no-terminate` می‌توانید اینستنس را فعال نگه دارید

### انتخاب GPU مناسب
- **RTX 3090**: 24GB VRAM، قیمت مناسب (توصیه شده)
- **RTX 4090**: 24GB VRAM، بهترین عملکرد
- **A4000/A5000**: 16GB VRAM، پایداری بالا

### 🌍 مکان جغرافیایی اینستنس
- **برای Binance**: حتماً از مناطق غیر آمریکایی استفاده کنید
- **توصیه شده**: EU یا Asia برای پینگ بهتر به سرورهای Binance
- **بررسی خودکار**: اسکریپت موقعیت IP را پس از ساخت اینستنس تأیید می‌کند

## 🔴 مدیریت دستی اینستنس‌ها

### نمایش اینستنس‌های فعال
```bash
vastai show instances
```

### خاموش کردن دستی اینستنس
```bash
vastai destroy instance INSTANCE_ID
```

### اتصال به اینستنس فعال
```bash
ssh -i ~/.ssh/vast_fix root@INSTANCE_IP
```

## ⚠️ نکات مهم

1. **پارامترهای ترید**: این اسکریپت هیچ پارامتر تریدی را تغییر نمی‌دهد
2. **کانفیگ فریمورک**: تنظیمات trading در فایل‌های خود فریمورک قرار دارند
3. **تایید نهایی**: اسکریپت در انتهای اجرا برای خاموش کردن اینستنس تایید می‌خواهد
4. **VRAM**: حداقل 16GB VRAM برای مدل‌های RL مورد نیاز است
5. **هزینه**: همیشه هزینه اینستنس‌ها را چک کنید و اینستنس‌های غیرضروری را خاموش کنید
6. **🌍 موقعیت جغرافیایی**: برای Binance حتماً از IP غیر آمریکایی استفاده کنید
7. **نصب پیشرفته‌ها**: تمام کتابخانه‌های RL به صورت خودکار نصب می‌شوند

## 🆘 پشتیبانی

در صورت بروز مشکل:
1. لاگ `vast_ai_launcher.log` را بررسی کنید
2. وضعیت اینستنس‌ها را با `vastai show instances` بررسی کنید
3. تنظیمات کانفیگ را بازبینی کنید
4. از حداقل 16GB VRAM اطمینان حاصل کنید
5. **برای Binance**: موقعیت جغرافیایی IP را در لاگ‌ها چک کنید
6. **نصب کتابخانه‌ها**: لاگ نصب پیشرفته‌ها را بررسی کنید